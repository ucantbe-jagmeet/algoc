#include <stdio.h>

int main(){
    int x = 10;
    int y = 3;
    float z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;
    // x+=3;
    // x-=3;
    // x+=3;
    // x/=2;

    printf("%d",x);

    return 0;
}