#include <stdio.h>
#include <math.h>

int main(){
    int x = 9; 
    // x = sqrt(x);
    // x = poq(x, 4);
    // x = round(x);
    // x = ceil(x);
    // x = floor(x);
    // x = abs(x);
    x = log(x);


    printf("%d", x);
    return 0;
}